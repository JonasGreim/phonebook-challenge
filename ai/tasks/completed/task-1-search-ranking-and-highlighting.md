# Completed task: Task 1 - Search ranking and highlighting

## Result

Search now ranks name and name-part starts ahead of other case-insensitive substring matches
before pagination. Displayed names highlight literal, non-overlapping matches safely and retain
their original text, IDs, and accessible names.

## Verification

- Local: the user confirmed that `npm run check` passed.
- Remote: the user confirmed that the committed and pushed CI pipeline passed.
